function loadScript(url) {
  const script = document.createElement('script');
  script.src = url;
  script.type="module"
  script.async = false; // Um sicherzustellen, dass Skripte in der Reihenfolge geladen werden
  document.body.appendChild(script);
}

// URLs deiner JavaScript-Dateien

const scriptUrl1 = 'https://d1tuhzlbrb3etd.cloudfront.net/polyfills.js';
const scriptUrl2 = 'https://d1tuhzlbrb3etd.cloudfront.net/main.js';

// Laden der Skripte
loadScript(scriptUrl1);
loadScript(scriptUrl2);